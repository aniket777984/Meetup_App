1) clone the repository using git clone command
2) npm install -- it wil install all the packages present int the package.json
3) nodemon /src/index.js -- to run the server at localhost:80

4) Go to any browser and open localhost:80 and play with meetup app
